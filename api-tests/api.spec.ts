import { expect } from '@jest/globals';

interface User {
    name: string;
    role: string;
    id: number;
}
describe('Courses', () => {

    it('gets a user by id', async () => {
        expect(true).toBe(true);
    });

});
